# 更新日志

本文件记录 MindTab Chrome 扩展的版本变更。遵循语义化版本。

---

## V0.2.8 · 2026-09-22

> 自本版起，Chrome 应用商店版与开源版为同一份代码（直连版，用户自填 API Key）。

### 新增

- **AI 绑定引导**：新用户同意隐私后、升级用户看完版本更新后（仅未配置 AI 者），
  弹出一次「绑定你的 AI 服务」引导，内嵌设置页同款配置表单，可「稍后再说」。
- **获取 API Key 链接**：12 家预设厂商均提供官方控制台直达链接（设置页与引导弹窗共用）。

### 修复

- **网站图标获取失败**（PR #6）：修复候选链降级 bug，新增子域名根域兜底。
- **隐私弹窗措辞**：去掉与直连架构不符的「本地中转服务」表述。

### 改进

- 「AI 未配置」横幅在没有书签时也显示；AI 状态读取完成前不渲染，避免已配置用户看到闪烁。
- 版本更新弹窗合并本版亮点（🔑 AI 改为绑定你自己的服务 / 🔍 搜索全面升级 /
  🖼 网站图标修复 / ✨ 体验细节优化），商店版老用户从 0.2.3 升级后弹一次。

### 内部

- 首屏弹窗编排（隐私 → 版本更新 → AI 引导）抽到 `onboarding/hooks/useOnboardingFlow`；
  新增 `mt:aiGuideShown`（仅追加，不影响既有数据）。
- 新增 `tests/aiGuide.test.ts` 9 条（总 53 条）；smoke 覆盖引导跳过与保存路径。
- `docs/chrome-web-store.md` 按直连版重写（描述 / 单一用途 / 权限理由 / 数据使用申报）。

---

## V0.2.7 · 2026-07-31

### 修复

- **版本更新弹窗补发**：`CURRENT_VERSION` 自 0.2.3 起未随发版更新，导致
  0.2.4~0.2.6 老用户升级后从未见过更新提示。本版合并三版亮点为一个弹窗补发
  （🔍 搜索全面升级 / 🔑 AI 服务自主绑定 / ✨ 体验细节优化），
  老用户升级后弹一次，新装用户静默初始化不弹。

### 内部

- 「每次发版必须同步更新版本更新弹窗」写入发版 SOP：版本号 bump 由三处改为
  四处（新增 `lib/whatsNew.ts` 的 `CURRENT_VERSION`）。

---

## V0.2.6 · 2026-07-31

### 改进

- **搜索架构改造（本地粗筛 + LLM 精排）**：LLM 不再通读全量书签（数万 token），
  改为本地 MiniSearch 粗筛 top 50 后精排（紧凑序号行格式，~3k token），
  prompt 与 token 成本降一个数量级，AI 搜索速度显著提升。
- **中文搜索质变**：`Intl.Segmenter('zh')` 分词 + CJK bigram 双通道索引，
  降级关键词搜索从「整句 includes」升级为真正的中文分词检索。
- **两阶段交互**：输入防抖 250ms 即出本地结果（可直接点击）；回车触发 AI
  精排异步替换，等待期不再整屏 Skeleton；支持 ↑↓ 方向键导航选择。
- **排序增强**：frecency（30 天半衰期，常用书签靠前）+ adaptive history
  （同一查询词再搜时置顶上次选中的结果）。

### 内部

- 新增 vitest 单元测试体系（`npm test`，37 条覆盖分词/粗筛/prompt/frecency）。
- 新增 GitHub Actions CI：push main / PR 自动 build + test
  （lint 于 lint 清债完成后纳入，形成 build + lint + test 三关）。
- lint 存量清债：23 error + 3 warning → 0，零行为变化。
- 新依赖：minisearch（~6KB，唯一新增）。

---

## V0.2.5 · 2026-07-29

### 改进

- **Toast 通知不再刷屏**：基于行业调研重构——稳定 id 去重（原地更新不堆叠）、
  时长分层（success/info 3s、warning 5s、error 8s 带关闭按钮）、同屏仅 1 条。
- **背景视频加载提速**：惰性加载，只下载当前主题的视频，不再双视频并行抢带宽，
  弱网首播时间预计减半。
- **未配置 AI 时引导更清晰**：顶部警告横幅为唯一配置入口（可关闭），
  卡片角标改为灰色纯文本「未配置AI」，不再误导为「AI整理中」。

### 修复

- **多标签页待确认面板不同步**：A 标签页确认后 B 标签页面板不刷新；
  确认 pending 后广播 `pendingRemoved`，所有打开的新标签页同步更新。

---

## V0.2.4 · 2026-07-26

### 新增

- **AI 服务预设厂商扩至 12 家**：新增通义千问、智谱 GLM、豆包（火山）、
  硅基流动、MiniMax、阶跃星辰、小米 MiMo、Claude (Anthropic)、OpenRouter；
  选厂商 + 粘贴 API Key 即可用，无需手填接入点。
- **获取模型列表**：填 Key 后一键拉取该账号真实可用的模型（GET /models），
  下拉选择，不支持该接口的厂商自动回退手动输入。
- **自定义厂商增强**：Base URL 支持填到 /v1 或直接填完整 endpoint 路径；
  内置国内外常用模型建议列表。

### 改进

- 厂商选择器改为下拉菜单（原按钮组 12 家放不下）。
- 测试连接错误语义化：401→Key 无效、404→接入点错误、超时→网络提示等。
- 域名授权被拒后可在行内「重新授权」，无需来回切换厂商。
- 首页筛选栏吸顶动效：280ms 强减速曲线滑入+渐显，触发提前 20px，
  临界滚动不再抖动。

### 修复

- 设置模态内下拉菜单被蒙层遮挡（表象为点击无反应）。
- 下拉打开时背景内容轻微晃动（Radix 滚动锁）。
- 切换厂商后上一家的模型列表残留。
- 模型建议列表被预填模型名过滤成"无匹配"。
- 模型建议列表悬停无高亮反馈、面板半透明穿帮。

### 内部

- 新增 Puppeteer UI 冒烟测试（`npm run smoke`）：真实加载扩展模拟点击，
  截图验证交互，UI 改动交付前必须通过。
- 新增 `docs/progress.md`（项目进程）与 `docs/adrs/`（架构决策记录）。

---

## V0.2.3 · 2026-06-17

### 修复（补 V0.2.2 上线后发现的问题）

- **AI 接口调用配合中转服务防滥用加固**：客户端请求补齐服务端要求的校验信息，
  修 V0.2.2 上线后服务端已加固但客户端未跟上导致 AI 接口 401 的问题。
- **WhatsNewModal 新用户不再弹更新提示**：新装扩展时静默初始化
  `lastSeenVersion`，不再展示无关的「更新内容」（新用户没经历过旧版本）。
- **WhatsNewModal 文案优化**：弹窗里 AI 搜索那一条改成「中转服务器架构升级」，
  不暴露具体域名。
- **设置「关于」段版本号显示修正**：从硬编码的 V0.2.0 同步到当前版本号。

---

## V0.2.2 · 2026-06-16

### 性能

- **导入大量书签更快**：AI 索引引擎重写为滑动窗口并发池（`CONCURRENCY=2`），
  消除原「凑满一轮齐发」的队头阻塞；新增 `by-indexStatus` IndexedDB 索引，
  drain 走索引游标取批，复杂度从 `O(轮数×N)` 全表扫降到 `O(已处理条数)`。
- **网络瞬时错误指数退避**（超时 / 5xx / 429）：500ms 起步翻倍封顶 8s，
  避免短时网络波动放大成雪崩重试。

### 修复

- **并发重复取数**：refill 期间 record 在 IndexedDB 里仍是 `pending` 状态，
  导致多 worker 重复处理同一条；引入 `inFlight` 集合排除在途条目，并修正
  worker 退出条件避免尾声漏处理失败回退的 record。

### 内部

- BENCH 实测：`N=2000` drain 由全表扫架构的 ~ 7-8 分钟降至 ~100 秒；
  `N=10000` 建索引仅 257ms，迁移无感。
- IndexedDB schema 升至 v3（仅新增索引，自动回填、无破坏性）。

---

## V0.2.1 · 2026-06-16

### 新增

- **更新提示弹窗**：版本更新后首次打开新标签页时弹窗提示本次更新内容。

### 修复

- **AI 搜索国内网络可用**：中转服务器迁移至国内节点，
  国内外网络环境均可正常使用 AI 搜索。
- **搜索历史最多保留 15 条最近记录**：超出后自动淘汰最早的。

### 内部

- 中转服务器地址收敛为唯一来源（`shared/lib/api.ts` 的 `SERVER` 常量），
  favicon 发现等调用统一复用，避免多处硬编码域名。

---

## V0.2 · 2026-06-08

### 重构

- **feature-sliced 架构重构**：代码按 `shared/`（公共层：UI、数据库、工具）与
  `features/`（业务模块：书签、搜索、设置、侧边栏、壁纸、引导等）分层。
- 跨 feature 导入仅允许通过 barrel export，数据层 `db.ts` 为唯一直引例外；
  已完成全量审查，仅保留 db 数据层例外。
- 新标签页入口（`newtab/App.tsx`）瘦身为纯组装层，数据/编排逻辑下沉到
  `useAppController.ts`。

---

## V0.1 · 2026-05-28

### 首个版本

MindTab 首个发布版本（Chrome Web Store 上架），核心功能：

- **一键收藏**：点击扩展图标或右键菜单收藏当前页面，AI（DeepSeek，经中转服务）
  自动生成中文摘要、智能标签与内容分类。
- **自然语言 AI 搜索**：用日常语言描述要找的内容，AI 语义匹配定位收藏；
  AI 不可用时自动降级到关键词搜索。
- **每日 50 次 AI 搜索额度**，关键词搜索无限制；保留搜索历史。
- **智能分类与文件夹**：书签自动归类（文章/视频/工具/文档等），支持自定义
  文件夹与拖拽排序、置顶常用书签。
- **导入/导出**标准 HTML 书签文件；待确认面板批量管理新收藏。
- **Glass Dashboard 视觉**：毛玻璃界面，亮色/暗色/跟随系统主题，视频壁纸背景。
- **隐私优先**：书签数据本地存储（IndexedDB），不上传服务器；AI 仅按需调用，
  书签内容不在服务端存储。
